function setup() {
  new Canvas(500, 200);

  // 设置背景颜色
  background("gray");

  // 设置文字大小
  textSize(50);

  // 设置文字颜色
  fill("white");

  // text(内容, x坐标, y坐标) 函数用于在画布上显示文本
  text("今天天气真不错", 0, 100);
}
